#define BOARD_ADDR 'f'          // board address, may be up to 16 bits
#define PHASECAL_OFFSET 143     // phase offset, in degrees
#define AMPCAL_OFFSET 8         // amplitude offset, in degrees (replace with table)
